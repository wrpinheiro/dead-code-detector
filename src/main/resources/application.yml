spring:
  jersey:
    application-path: /api

endpoints:
  enabled: false
  info:
    enabled: true
  health:
    enabled: true
  metrics:
    enabled: true

app:
  analyzer:
    repositoriesDirectory: ${ANALYZER_REPO_DIR:/Users/wrpinheiro/.deadCodeDetection/repos}
